import type { StoreContext } from './store-context.svelte.js';
export interface SinaPlugin<Options extends object> {
    /**
     * Setup the store on initialization.
     *
     * Any arbitrary side effects are permitted with respect to the data store
     * within the setup function. It will only run once when the data store has
     * been brought into context within the component tree..
     *
     * @param store A reference to the data store instance.
     */
    setup?(store: StoreContext<object> & object): void;
    get?: ProxyHandler<StoreContext<object> & object>['get'];
    set?: ProxyHandler<StoreContext<object> & object>['set'];
    has?: ProxyHandler<StoreContext<object> & object>['has'];
    options: Options;
}
export interface PluginAPI<Options extends object> {
    new (parameters: {
        options: Options;
    }): SinaPlugin<Options>;
}
