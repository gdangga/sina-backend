export interface StoreContext<T extends object> {
    /** Debug label */
    $label?: string;
    /**
     * A map of data store properties and their respective type.
     *
     * The property may either be plain old state, a derived getter property or
     * a method that performs an action.
     */
    get $properties(): {
        [Key in keyof T]: 'state' | 'getter' | 'action';
    };
}
export declare const createPropertyMapper: <T extends object>(state: T) => () => { [Key in keyof T]: "state" | "getter" | "action"; };
export declare const createStoreContext: <T extends object>(parameters: {
    state: T;
}) => StoreContext<T>;
